import wollok.game.*
import nivelesycfg.*
import personaje.*
import enemigos.*
import background.*

program juego {
	game.title("AlgunNombreBueno")
	game.width(20)
	game.height(10)
	game.cellSize(50)
	game.boardGround("background_black.png")
	nivel0.iniciar()
	game.showAttributes(personajePrincipal)
	game.showAttributes(spectrum01)
	game.showAttributes(ogre01)
	config.asignarTeclas()
	config.movimientoEnemigos()
	config.recargaEnergia()
	config.configurarColisiones()
	game.start()
}
