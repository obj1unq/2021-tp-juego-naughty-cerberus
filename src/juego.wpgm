import wollok.game.*
import nivelesycfg.*
import personaje.*
import enemigos.*

program juego {
	game.title("Conurvania")
	game.width(20)
	game.height(10)
	game.cellSize(50)
	game.boardGround("fondo.png")	
	nivel0.iniciar()
	game.showAttributes(personajePrincipal)
	game.showAttributes(spectrum02)
	config.asignarTeclas()	
	config.movimientoEnemigos()
	config.recargaEnergia()	
	config.configurarColisiones()
	game.start()
	
}
